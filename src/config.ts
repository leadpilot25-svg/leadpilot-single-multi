// Global Application Mode Switch
// 'single': Personal CRM mode (Only one user, sees own leads, hides roles and multiple agents info)
// 'multi': Corporate CRM mode (Admin + multiple agents, role-based filtering, assignedTo fields)
export const APP_MODE: "single" | "multi" = "single";
